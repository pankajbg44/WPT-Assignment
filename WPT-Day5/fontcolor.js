function applyColor(c) {
    let ptag = document.querySelector("p");
    ptag.style.color = c;
  }

  function applyColorDiv(d) {
    let ptag = document.querySelector("div");
    ptag.style.color = d;
  }
  
  function applyFont(a) {
    let ptag = document.querySelector("p");
    ptag.style.fontSize = a;
  }

  function applyFontDiv(f) {
    let ptag = document.querySelector("#p1");
    ptag.style.fontSize = f;
  }

  function applyFontp1(f) {
    let ptag = document.querySelector("div");
    ptag.style.fontSize = f;
  }